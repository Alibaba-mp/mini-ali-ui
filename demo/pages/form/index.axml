<view>
  <view style="margin-top: 10px;" />
  <list>
    <view class="title">表单</view>
    <input-item data-field="input" 
      clear="{{true}}" 
      value="{{input}}" 
      className="dadada" 
      placeholder="暗提示" 
      onInput="onItemInput" 
      onBlur="onItemBlur" 
      onConfirm="onItemConfirm" 
      onClear="onClear">
      标题
    </input-item>
    <picker-item data-field="picker" 
      placeholder="暗提示" 
      value="{{bank}}" 
      onPickerTap="onPickerTap">
      标题
    </picker-item>
    <verify-code data-field="verifyCode" 
      onInput="onItemInput" 
      value="{{verifyCode}}" 
      onClear="onClear" 
      countDown="{{10}}" 
      onSend="onSend"></verify-code>
    <long-password data-field="longPassword" 
      placeholder="" 
      type="text" 
      value="{{longPassword}}" 
      clear="{{true}}" 
      onInput="onItemInput" 
      onClear="onClear" />
    <view class="title">多行输入</view>
    <multi-liner data-field="area" 
      placeholder="字数统计↘" 
      type="text" 
      value="{{name}}" 
      clear="{{true}}" 
      onInput="onItemInput" 
      onClear="onClear" 
      last="{{true}}" 
      auto-height="{{true}}" />
  </list>
  <view style="margin: 10px;" />
</view>