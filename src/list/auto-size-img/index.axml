<view class="am-auto-sizer-container" 
  style="{{`width: ${size}; height: ${size};`}}">
  <view class="am-auto-sizer-content" 
    style="{{(src ? `background-image: url('${src}'); background-color: transparent` : '')}}" />
</view>