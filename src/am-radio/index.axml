<view class="am-radio">
  <radio
    class="am-radio-value"
    value="{{value}}"
    checked="{{checked}}"
    disabled="{{disabled}}"
    onChange="onChange"
    color="{{color}}"
    id="{{id}}"
    />
  <view class="am-radio-synthetic"></view>
</view>