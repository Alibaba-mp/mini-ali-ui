<view class="am-radio">
  <radio
    class="am-radio-value"
    value="{{value}}"
    checked="{{checked}}"
    disabled="{{disabled}}"
    onChange="onChange"
    color="{{color}}"
    id="{{id}}"
    />
    <am-icon className="am-radio-synthetic" type="check"/>
</view>